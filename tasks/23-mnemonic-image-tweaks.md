# P3 · Mnemonic image tweaks — a later art pass

**Status: logged, not urgent.** From a full image-vs-story review of every kana
mnemonic drawing (both scripts). The stories and object tags are now correct;
these are the DRAWINGS that read a little off. Each shows its object clearly —
the gap is the shape→kana link or a small story/image drift — so none is a
blocker, they're candidates for a redraw when the art gets another pass.

The story-side fixes this review turned up are already DONE in `mnemonics.ts`
(と splinter-not-foot, ん pen, イ standing eagle, せ object→centipedes, and the
earlier を roller coaster). What's left here is purely the images.

## Redraw candidates (shape link weak or story drift)

- **は (ha):** the drawing is the literal letters "Ha", not a person laughing.
  Owner's call — it carries the idea and the shape, kept on purpose for now.
- **ん (n):** the fountain PEN dominates the frame; the cursive "n" it draws is
  small behind it. Story now names the pen, so it's consistent — but the art
  could foreground the "n" more.
- **よ (yo):** reads as the よ glyph itself more than a bobbing yo-yo.
- **イ (i, katakana):** eagle is drawn standing/static. Story was rewritten to a
  standing eagle (wing sweeping down = the diagonal), so it's reconciled — a
  diving/slanting pose would map to イ more strongly if ever redrawn.
- **ワ (wa, katakana):** the "giant question mark" the story wants is drawn as
  the ワ glyph shape. Owner judged this fine (it reads as a question mark).

## Soft — recognizable object, loose shape link (lowest priority)

- **ナ** (narwhal), **ル** (kangaroo + tree), **ユ** (kid in icy bath): the object
  reads clearly but the kana shape is only loosely traced.
- **サ:** works, but the story's "hidden knife" isn't in the drawing.
- **ふ (hiragana):** minor — the swirl reads a touch like a river; it is Fuji,
  owner confirmed fine.

## Not an issue (reviewed, left as-is by owner)

- **あ:** object pill "father" with an acrobat drawn — intentional; the father
  watches from off-scene and survives as the /a/ sound ("f·a·ther"). The story
  need not describe the image exactly.

Ignored during review (in progress): hiragana な (being redrawn), katakana ム and
ヒ (not authored).
