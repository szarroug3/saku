# 23 — Results-table presentation boxes should left-align 🟠
In the by-word results table (#04), the presentation boxes don't line up — a longer glyph (あなた) pushes its box right (image 8). Give the glyph/label column a fixed width so every row's boxes start at the same x.
