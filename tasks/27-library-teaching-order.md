# 27 — Library: teaching order, ranged groups, full word list 🟠
- Show everything in the Library in TEACHING (curriculum) order, broken into ranges (1–50, 51–100, …), the way kana groups already are — for every subject.
- Drop the separate "Everyday words" section; words become ordered ranged groups like the rest.
- Show the ENTIRE word list, not the current top-120 cap. Sam explicitly wants to SEE the performance impact of rendering all of them (so: render all, and we measure — don't pre-optimize it away).
