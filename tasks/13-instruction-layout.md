# 13 — Move instruction below halo, white, drop the small labels 🔵 in progress

On the drill card: move the full instruction ("Type what the kanji means.") from ABOVE the halo to BELOW it, make it white (currently muted gray), and remove the smaller redundant labels — the context sublabel ("meaning") and the direction hint ("Answer in English."). The full instruction already says what to do.

Built on the `quiz-clarity` branch (which owns the instruction render). Judgment call: keep the mechanical "Romaji turns into kana as you type" hint (it explains the input, not the task) — flag for Sam.
