import { PageTitle } from "@/components/ui";

// TODO(agent:settings): retries / timer / toggles / voice pills.
// See CONVERSION_PROMPT.md "Settings page".
export default function SettingsPage() {
  return (
    <PageTitle title="Settings" sub="Applies to every quiz · saved automatically" />
  );
}
