// Sign in, under the sky: the account page, at the path the app's session
// handling sends a signed-out visitor to.

export const metadata = { title: "Sign in" };

export { default } from "../account/page";
