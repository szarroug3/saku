import { PageTitle } from "@/components/ui";

// TODO(agent:home): quiz builder — mode/direction/style/length rows with
// mode-dependent dimming, Kana-Pro-style character picker, live subtitle,
// Start Quiz button. See CONVERSION_PROMPT.md "Home (quiz builder)".
export default function HomePage() {
  return <PageTitle title="Kana quiz" sub="Quiz builder coming up." />;
}
