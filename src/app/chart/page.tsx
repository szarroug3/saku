import { PageTitle } from "@/components/ui";

// TODO(agent:stats): Tofugu links, live search, speakable kana grid.
// See CONVERSION_PROMPT.md "Kana chart".
export default function ChartPage() {
  return <PageTitle title="Kana chart" sub="Japanese · romaji · mnemonic" />;
}
